{
  "$schema": "http://json-schema.org/schema#",
  "type": "object",
  "properties": {
    "apiKey": {
      "title": "API Key",
      "description": "SendGrid API key, created under the [\"API Keys\" tab](https://app.sendgrid.com/settings/api_keys) of the Settings page.",
      "type": "string",
      "minLength": 32
    }
  },
  "additionalProperties": false,
  "required": ["apiKey"]
}
