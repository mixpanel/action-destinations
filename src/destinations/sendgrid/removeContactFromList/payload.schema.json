{
  "$schema": "http://json-schema.org/schema#",
  "title": "Remove Recipient from List",
  "description": "Remove a recipient from a list given their email address",
  "type": "object",
  "properties": {
    "list_id": {
      "title": "List ID",
      "description": "The ID of the SendGrid list to remove the user from.",
      "type": "string",
      "autocomplete": true
    },
    "email": {
      "title": "Email Address",
      "description": "Email address of the user to be removed from the list.",
      "type": "string"
    }
  },
  "required": ["list_id", "email"]
}
