{
  "$schema": "http://json-schema.org/schema#",
  "title": "SendGrid: Create or update a recipeint",
  "description": "Update an existing recipient or create them if they do not exist.",
  "type": "object",
  "properties": {
    "email": {
      "description": "Email address of the user to be updated or created.",
      "type": "string"
    },
    "custom_fields": {
      "description": "Additioinal fields to store on the recipeint record.",
      "type": "object",
      "default": {}
    }
  },
  "required": [
    "name"
  ]
}