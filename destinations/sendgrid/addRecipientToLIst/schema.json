{
  "$schema": "http://json-schema.org/schema#",
  "title": "SendGrid: Add recipient to list",
  "description": "Add a recipient to a list given their email address",
  "type": "object",
  "properties": {
    "list_id": {
      "description": "The ID of the SendGrid list to add the user to.",
      "type": "number"
    },
    "email": {
      "description": "Email address of the user to be added to the list.",
      "type": "string"
    }
  },
  "required": [
    "list_id",
    "email"
  ]
}