{
  "$schema": "http://json-schema.org/schema#",
  "title": "SendGrid: Delete contact",
  "description": "Delete a contact from SendGrid.",
  "type": "object",
  "properties": {
    "email": {
      "title": "Email address",
      "description": "Email address of the contact to delete.",
      "type": "string"
    }
  },
  "required": [
    "email"
  ]
}
