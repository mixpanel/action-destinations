{
  "$schema": "http://json-schema.org/schema#",
  "type": "object",
  "properties": {
    "url": {
      "title": "URL",
      "description": "Table-specific API URL that records will be sent to.",
      "type": "string",
      "format": "uri"
    }
  },
  "required": ["url"]
}
