{
  "$schema": "http://json-schema.org/schema#",
  "title": "Customer.io: Track an anonymous event",
  "description": "Track an anonymous event (an event not tied to a known customer).",
  "type": "object",
  "properties": {
    "name": {
      "description": "Event name.",
      "type": "string"
    },
    "data": {
      "description": "Custom data to include with the event.",
      "type": "object"
    }
  },
  "required": [
    "name"
  ]
}