{
  "$schema": "http://json-schema.org/schema#",
  "title": "Customer.io: Create or update device",
  "description": "Update a person's device in Customer.io or create it if it does not exist.",
  "type": "object",
  "properties": {
    "person_id": {
      "title": "Person ID",
      "description": "ID of the person that this device belongs to.",
      "type": "string"
    },
    "device_id": {
      "title": "Device ID",
      "description": "Unique ID for this device.",
      "type": "string"
    },
    "platform": {
      "title": "Platform",
      "description": "The device platform.",
      "type": "string",
      "enum": [
        "ios",
        "android"
      ]
    },
    "last_used": {
      "title": "Last used",
      "description": "Timestamp for when the device was last used. Default is current date and time",
      "type": "string"
    }
  },
  "required": [
    "person_id",
    "device_id",
    "platform"
  ]
}
